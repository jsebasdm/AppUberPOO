// Clase Paypal (Hereda de la clase Pago)
class Paypal extends Payment {
    //Método constructor
    constructor(email) {
        super()
        //Atributos o propiedades de la clase
        this.email = email;
        this.id
    }
}